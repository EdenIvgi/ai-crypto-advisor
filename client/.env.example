# Copy to client/.env for local development. Vite only exposes variables prefixed VITE_.

# Origin of the API. No trailing slash — paths are appended directly.
# Locally this is the Express server; in production it is the Render URL.
VITE_API_BASE_URL=http://localhost:4000
